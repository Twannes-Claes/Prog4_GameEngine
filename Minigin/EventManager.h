#pragma once
#include <functional>
#include <string>

#include "Singleton.h"

namespace Monke
{
    /*class EventManager final : Singleton<EventManager>
    {

    public:

        void AddListener(const std::string& eventName, void (*listener)());
        void RemoveListener(const std::string& eventName, void (*listener)());
        void TriggerEvent(const std::string& eventName);

    private:

        std::unordered_map<std::string, std::function<void()>> m_EventMap;
    };*/
}





